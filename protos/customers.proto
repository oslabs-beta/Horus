syntax = 'proto3';

service CustomersService {
    rpc CreateCustomer (Customer) returns (Customer){}
    rpc GetCustomers (Empty) returns (CustomerList){}
    rpc DeleteCustomer (CustomerID) returns (Empty) {}
}

message Customer {
    int32 id = 1;
    string name = 2;
    string age = 3;
    string address = 4;
}

message Empty {}

message CustomerList{
    repeated Customer names =1;
}

message CustomerID {
    int32 id = 1;
}
